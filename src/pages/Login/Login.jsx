import { Helmet } from "react-helmet-async";

const Login = () => {
    return (
        <div>
            <Helmet>
                <title>Login | BuildMinder</title>
            </Helmet>
        </div>
    );
};

export default Login;