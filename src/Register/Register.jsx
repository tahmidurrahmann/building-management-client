import { Helmet } from "react-helmet-async";

const Register = () => {
    return (
        <div>
            <Helmet>
                <title>Register | BuildMinder</title>
            </Helmet>
        </div>
    );
};

export default Register;